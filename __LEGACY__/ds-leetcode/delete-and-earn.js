/**
 * @param {number[]} nums
 * @return {number}
 */
var deleteAndEarn = function(nums) {
    // Dynamic Programming
    // Time: O(n)
    // Space: O(n)
    
    // Track the last two values
    // Count Table
    // Time: O(n)
    // Space: O(n)
    
};